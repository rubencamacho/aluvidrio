{% extends '::frontend.html.twig' %}

{% block content %}

	<h1>Operario horas</h1>

	    <ol class="breadcrumb">
        <li> <a href="{{asset('aluvidrio')}}">Inicio</a></li>
        <li><a href="{{asset('informe/operarios')}}">Informe Operarios</a></li>
        <li class="active">Informe Operario</li>
    </ol>

<div class="form">
    
    {{ form_start(form) }}
    <div class="input-group">

        {{form_start(form)}}
        {{form_end(form)}}

        <button type="submit" name="" class="btn btn-default button-form">Seleccionar Fechas</button>
    </div>     
     {{ form_end(form) }}

</div>
    <br><br>
     <span>Un total de: <strong>{{total}}</strong> horas</span>

{% endblock %}